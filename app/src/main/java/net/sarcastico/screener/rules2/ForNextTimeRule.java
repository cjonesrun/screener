package net.sarcastico.screener.rules2;

import net.sarcastico.screener.utils.Time;

public abstract class ForNextTimeRule /*extends BasicTimeRule*/{

	/*public ForNextTimeRule(String name, String desc, int priority, Object t, int quantity)
	{
		super(name, priority, Time.now(), Time.now());
		//super(name, priority, LocalTime.now().minus(1, ChronoUnit.MILLIS), LocalTime.now().plus(quantity, t));
	}
	*/
	//public static final void main(String[] s) throws Exception {
		// new java.time.DayOfWeek
		// new java.time.Month
		
		/*LocalDateTime ldt = LocalDateTime.now();
		System.out.println(ldt.get(ChronoField.DAY_OF_WEEK));*/
		
		
		
	//}

	
	
	
}
